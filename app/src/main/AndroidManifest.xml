<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <package android:name="com.termux" />
        <package android:name="com.shopee.tw" />
        <package android:name="com.shopee.ph" />
        <!-- 【2026-08-28新增】蝦皮批次上架完自動啟動FB App接續跑FB上架，需要先在這裡宣告
             要查詢的套件名稱，否則Android 11+的package visibility限制會讓
             getLaunchIntentForPackage()永遠回傳null，誤判成「FB未安裝」。 -->
        <package android:name="com.facebook.katana" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <!-- 階段2上架自動化：scanUploadCandidates() 需要直接用 File API 讀取 CaptionQueue 底下
         各商品資料夾的 meta.json（非媒體類型檔案）。純 READ_MEDIA_IMAGES/READ_EXTERNAL_STORAGE
         在 Android 10+ scoped storage 下只保證媒體類型檔案的存取，.json/.txt 這類一般檔案即使是
         App自己寫入的，仍可能被拒讀（EACCES）。加上這個特殊權限並在設定頁引導使用者開啟，
         繞過這個限制，寫入行為不受影響（本來就正常）。 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- 簡易模式「生成影片」功能：透過Termux的RUN_COMMAND介面在背景觸發batch_generate.py，
         使用者完全不用打開Termux。需要Termux那邊在~/.termux/termux.properties設定
         allow-external-apps=true（一次性設定），且Termux App本身要是已安裝、允許背景執行的狀態。 -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <application
        android:allowBackup="true"
        android:label="蝦皮擷取器"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="true">

        <activity
            android:name=".SimpleModeActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 開發／進階設定畫面（校正、偵測、上架自動化細部設定等）。不再是啟動畫面，
             改成從SimpleModeActivity角落的「進階設定」連結用明確Intent開啟，
             一般使用者（親友）操作簡易模式完全不會看到這個畫面。exported保留true
             是因為要能被明確Intent從SimpleModeActivity開啟。 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ClipboardBridgeActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:screenOrientation="unspecified" />

        <service
            android:name=".ShopeeAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".FloatingButtonService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <receiver
            android:name=".TermuxResultReceiver"
            android:exported="false" />

        <!-- 「檢查影片」畫面要把output.mp4交給系統影片播放器開啟，
             Android 7+需要透過FileProvider包成content://路徑才能安全跨App分享檔案。 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.tagcopy.shopeecapture.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
